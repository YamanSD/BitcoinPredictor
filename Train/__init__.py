from .linear_regression import load as lr_load, train as lr_train, test as lr_test, scale as lr_scale
from .logistic_regression import load as lgr_load, train as lgr_train, test as lgr_test, scale as lgr_scale
from .elastic_linear import load as elr_load, train as elr_train, test as elr_test, scale as elr_scale
